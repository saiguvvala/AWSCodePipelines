{
    "Parameters": {
        "Prefix": "{PARAM_PREFIX}",
        "Environment": "{PARAM_ENVIRONMENT}",
        "BucketName": "{PARAM_BUCKETNAME}"
    },
    "StackPolicy": {
        "Statement": [
            {
                "Effect": "Allow",
                "Action": "Update:*",
                "Principal": "*",
                "Resource": "*"
            }
        ]
    }
}